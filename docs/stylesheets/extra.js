// Exemplo de alertas mais interativos
document.querySelectorAll('.devops-btn').forEach(function(button) {
  button.addEventListener('click', function() {
    // Exibe um alerta simples ao clicar
    alert('Ação DevOps clicada com sucesso!');
  });
});
